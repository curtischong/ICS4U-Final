import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * One of the Enemies in the Game, trying to prevent the player from advancing!
 * 
 * @author 
 * @version (a version number or a date)
 */
public class CatEnemy extends Actor
{

    /**
     * Act - do whatever the CatEnemy wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public void act() 
    {
        // Add your action code here.
    }
    
    /*private void findTarget (){//find the target that the enemy is moving toward (hero)
        targetHero = getWorld().getObjects(Hero.class).get(0); //assigns the target to the hero in the world
        targetY = targetHero.getY(); //get's the target's Y position
        targetX = targetHero.getX();//get's the target's X position

    }*/
}
